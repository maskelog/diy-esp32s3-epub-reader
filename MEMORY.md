# MEMORY

이 문서는 이 저장소에서 작업할 때 에이전트가 따라야 하는 공통 작업 원칙을 정리한 메모입니다.
Codex, Claude Code, Gemini Code 등 다른 코딩 에이전트도 동일하게 참고할 수 있도록 작성합니다.

## 저장소와 원격

- 작업 저장소는 `diy-esp32s3-epub-reader` 기준으로 유지한다.
- 커밋과 푸시는 포크된 개인 원격에서만 진행한다.
- 기본 푸시 대상은 `origin`이다.
- `upstream`에는 직접 푸시하지 않는다.
- 다른 원격이 있더라도 사용자 지시가 없으면 포크 원격 외에는 푸시하지 않는다.

## M5Paper 관련 원칙

- `PNGdec`, `epdiy`, `pngle`는 M5Paper 출력 경로에서 필요한 구성으로 간주한다.
- 위 구성요소는 단순 정리 대상이나 미사용 코드로 가정하지 않는다.
- M5Paper 동작, 렌더링, 이미지 출력 관련 수정 시 이 의존성과 연동 경로를 보존한다.

## 커밋 원칙

- 사용자가 별도로 요청하지 않으면 작업 범위를 임의로 넓히지 않는다.
- 커밋 전에는 어떤 파일이 함께 포함되는지 반드시 확인한다.
- Windows 예약 이름 문제를 일으키는 파일(예: `nul`)은 무리하게 스테이징하지 않는다.
- 푸시는 항상 현재 작업 브랜치를 포크 원격으로 올린다.

## 협업 원칙

- 기존 사용자 변경사항을 함부로 되돌리지 않는다.
- 저장소 구조를 바꾸는 작업은 사용자 의도가 분명할 때만 진행한다.
- 보드별 수정 요청이 있으면 해당 보드 범위만 건드리고 다른 보드 변경은 포함하지 않는다.

