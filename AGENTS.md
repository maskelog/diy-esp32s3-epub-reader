# AGENTS.md — 공통 에이전트 규칙

과거 세션(Claude Code / Codex 로그 약 2,100건)에서 사용자가 반복 지적한 내용을 규칙화한 문서.
Codex, Claude Code, Gemini Code 등 이 저장소를 만지는 모든 에이전트에 적용된다.
저장소 고유 규칙(원격·푸시 대상, M5Paper 의존성 보존)은 `MEMORY.md`를 먼저 읽는다.

## 1. 확인 먼저, 수정은 그다음

- 사용자가 파일·로그·URL·이미지를 언급하면 **수정 전에 반드시 그것부터 읽는다.**
  ("~확인하고 수정" 지시를 사용자가 130회 이상 반복해야 했다.)
- 디버깅 요청 시 저장소 루트의 `debug.txt`부터 읽는다. 사용자는 기기 시리얼 로그를
  이 파일에 붙여넣는 방식으로 일한다.
- 단, 확인만 반복하지 않는다. 원인이 파악되면 바로 수정으로 넘어간다.
  ("계속 확인만 하지마"라는 지적이 있었다.)

## 2. "여전히 안됨"을 만들지 말 것

- 검증하지 않은 수정을 "해결됨/성공"이라고 보고하지 않는다.
  (성공 보고 후 실제로는 실패한 사례가 있고, "여전히 안됨" 재보고가 45회 이상 있었다.)
- 같은 증상으로 수정이 2회 실패하면 같은 지점을 계속 패치하지 말고,
  근본 원인 재진단부터 다시 시작한다.
- 하드웨어 동작(E-ink 표시, 터치, 슬립, WiFi 업로드 등)은 사용자가 직접 기기에서
  검증한다. 에이전트는 "빌드 통과"까지만 사실로 보고하고 기기 동작은 단정하지 않는다.
- 코드 변경 후에는 항상 빌드로 검증한다: `pio run -e paper_s3_idf` 또는 `pio run -e m5paper`
  (수정한 보드에 해당하는 env).

## 3. 커밋 규칙

- **커밋·푸시는 명시적으로 요청받았을 때만 한다.** ("커밋하지마"를 여러 번 말하게 하지 않는다.)
- 커밋 전 스테이징 목록을 확인하고, 다음은 요청 없이 절대 포함하지 않는다:
  - 개인 배경 이미지: `bg*.png`, `bg*.jpg` (사용자가 명시적으로 커밋 금지함)
  - `debug.txt`, 빌드 산출물(`build/`, `firmware*`), 실험용 스크립트(`fix_assembler.py` 등)
- 커밋 메시지는 conventional commits (feat / fix / chore / docs / refactor).
- 사용자가 커밋 메시지나 문서를 한국어로 요청하면 그 세션 내내 유지한다.
  ("한국어로"를 매번 다시 말하게 하지 않는다.)

## 4. 범위와 되돌리기

- 요청받은 것만 한다. 보드별 수정 요청이면 해당 보드 범위만 건드린다.
- 사용자의 기존 변경사항을 임의로 되돌리거나 정리하지 않는다.
- 실패 시 되돌리기 쉬운 작은 단위로 변경한다.
  (수정이 얽혀서 사용자가 전체 롤백을 해야 했던 사례가 있다.)
- 수동으로 시도할 수 있게 남겨달라는 요청이 있으면 파일은 두고 커밋만 하지 않는다.

## 5. 언어

- 답변·설명·정리 문서는 한국어로 쓴다. 기술 용어는 원어 그대로 사용한다.
